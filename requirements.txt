# Dependencies for the Streamlit Cloud web demo (streamlit_app.py).
# CLI users should `pip install pi-coding-agent` instead (see README).
streamlit>=1.32
anthropic>=0.40
openai>=1.55
pandas>=2.0
python-pptx>=0.6.21
openpyxl>=3.1
